{
  "id": "asense",
  "name": "aSense",
  "description": "Physical context for your AI agent — location, movement, activity, and nearby devices from your iPhone's sensors.",
  "version": "0.1.0",
  "skills": ["./skills/asense"],
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "server": {
        "type": "string",
        "description": "aSense worker URL (sets ASENSE_SERVER)"
      },
      "deviceId": {
        "type": "string",
        "description": "Device UUID from the aSense iOS app (sets ASENSE_DEVICE_ID)"
      },
      "apiToken": {
        "type": "string",
        "description": "API token for the aSense worker (sets ASENSE_API_TOKEN)"
      },
      "encryptionKey": {
        "type": "string",
        "description": "Base64 AES-256 encryption key from the aSense iOS app (sets ASENSE_ENCRYPTION_KEY)"
      }
    }
  },
  "uiHints": {
    "server": {
      "label": "Server URL",
      "placeholder": "https://sense.momstudios.com"
    },
    "deviceId": {
      "label": "Device ID",
      "placeholder": "A1B2C3D4-..."
    },
    "apiToken": {
      "label": "API Token",
      "placeholder": "oT-DBMjl..."
    },
    "encryptionKey": {
      "label": "Encryption Key",
      "placeholder": "Base64 key from iOS app Settings"
    }
  }
}
